
# Deploy to Firebase Hosting (one-time steps after extracting)

1) Make sure you have the Firebase CLI:
   npm install -g firebase-tools

2) Login once:
   firebase login

3) From this folder (where firebase.json is), run:
   firebase deploy

That's it — your site will be live at https://<your-project>.web.app
(Select project 'travel-416ff' when prompted.)
